# 채택 기준 (2026-07-24 확정)

이 리포트가 추천하는 것은 **지금 바로 안전하게 붙일 수 있는 것**뿐이다.
아래 게이트를 통과하지 못하면 순위에 올리지 않는다.

## 하드 게이트 (하나라도 걸리면 제외)

| # | 게이트 | 제외 대상 |
|---|---|---|
| G1 | **보안** | 알려진 CVE·샌드박스 탈출 이력, 광범위 자격증명 요구, 임의 원격코드 실행, 이중용도 공격도구, **서명 없는 실행 파일 동봉**(아래 스캔 절차) |
| G2 | **비용** | 유료·크레딧 소진형·무료티어 만료형. 완전 무료 또는 로컬 실행만 통과 |
| G3 | **라이선스** | GPL/AGPL/상용/독점, **라이선스 불명**. 통과 목록은 산출물 유형별로 아래에 따로 둔다 |
| G4 | **설치 부담** | 별도 바이너리·서버·Docker·시스템 데몬 설치 필요 |

G3의 "불명"은 통과가 아니라 **제외**다. 확인 못 한 것을 추천하지 않는다.

### G1 바이너리 스캔 — 저장소가 아니라 **계정 단위** (2026-07-31 개정)

마크다운 배포물에 서명 없는 실행 파일이 동봉되는 사례가 확인됐다. 채택 전 아래를 돌린다.
**실행 파일은 다운로드·실행하지 않는다** — 메타데이터만으로 G1 판정에 충분하고, 악성 여부는 단정하지 않는다.

```bash
# 1단계: 대상 저장소
gh api "repos/{owner}/{repo}/git/trees/HEAD?recursive=1" \
  --jq '[.tree[]|select(.type=="blob")|.path]
        | map(select(test("[.](exe|dll|so|dylib|bin|msi|jar|pyd|wasm|zip|7z|gz|ps1|bat|cmd)$";"i")))
        | if length==0 then "CLEAN" else "BINARIES: "+join(", ") end'

# 2단계: 같은 소유자의 다른 저장소 전부 (1단계만으로는 부족하다)
for n in $(gh api "users/{owner}/repos?per_page=100&type=owner" --jq '.[].name'); do
  printf "%-40s " "$n"
  gh api "repos/{owner}/$n/git/trees/HEAD?recursive=1" \
    --jq '[.tree[]|select(.type=="blob")|.path]
          | map(select(test("[.](exe|dll|msi|scr|bat|cmd|ps1)$";"i")))
          | if length==0 then "clean" else "*** "+join(", ") end' 2>/dev/null
done
```

**2단계가 필요한 이유(2026-07-31 실측)**: 저장소 단위 스캔은 한 계정에서 1건만 잡았으나,
계정 단위로 넓히니 **동일 블롭(SHA `79d051352df6`, 804,688B)이 저장소 6개에 4개 파일명으로**
들어 있었다(합계 약 2,079★·356fork). 놓친 5개가 1,534★를 갖고 있었다.

**한 번 지목한 계정은 채택 심사 때 배제 목록으로 조회한다.** 계정 단위로 제외하는 이유는
캠페인이 저장소 하나로 끝나지 않기 때문이다(2026-08-02 실측: 이틀 뒤 7번째 저장소, 08-03에 8번째).
현재 배제 목록: `0xwilliamortiz`.

**⚠ 배제 목록은 조회용이지 취재 대상이 아니다 (2026-08-05 사용자 지시로 개정).**
개정 전 규칙은 *"재스윕 목록에 고정한다"* 였고, 그 결과 **매일 같은 계정을 훑어 그 결과를 매일 실었다.**
같은 대상이 **6판 연속 1위**를 차지했고(07-31·08-02·08-03·08-04·08-05), 그중 스택 결정을 바꾼 것은
**07-31 1건뿐**이다 — *"이 계정의 저장소를 설치하지 마라, 패턴은 이렇게 잡는다."* 나머지는 같은 결론에
붙은 후속 관측이었다. **가치와 무관하게 매일 내용이 생기는 규칙을 세워 두면 리포트가 연재가 된다.**

따라서 배제 목록의 사용법은 하나다 — **채택 후보가 그 계정 소속인지 조회하고, 맞으면 제외한다.**
목록을 주기적으로 훑어 변화를 보고하지 않는다. 상세는 아래 "취재 종결 규칙".

판별 요령:

- **파일명이 달라도 블롭 SHA가 같으면 같은 파일이다.** 각 저장소에 어울리는 이름으로 위장한다.
- **저장소 목적과 무관한 실행 파일**이 가장 확실한 신호다(CUDA 커널 저장소의 `vibecodecalc.exe`).
- 유명 프로젝트명에 `-improved` 등을 붙인 **이름 스쿼팅**을 함께 본다.
- **별 수는 검증이 아니다.** 별 수 대비 팔로워·계정 생성일·저장소 생성 간격을 함께 본다.

**불투명 바이너리와 읽을 수 있는 스크립트를 구분한다 (2026-08-02 교정)**

스캔에 걸린 파일을 두 부류로 나눈다. 확장자가 걸렸다는 것만으로 제외하지 않는다.

| 부류 | 확장자 | 처리 |
|---|---|---|
| **불투명 바이너리** | `exe` `dll` `so` `dylib` `msi` `jar` `wasm` | 내용 확인 불가 → **G1 제외 후보**. 다만 판정 근거는 아래 "부르는 코드"에서 확보한다 |
| **읽을 수 있는 스크립트** | `ps1` `bat` `cmd` `sh` `js` `mjs` `py` | **읽고 판정한다.** 그 자체로는 제외 사유가 아니다 |

**바이너리를 만나면 그것을 실행하는 코드를 찾아 읽는다.** 바이너리는 판독 불가여도 설치 스크립트·훅 설정은 대개 읽을 수 있고, 판정의 실질은 거기서 나온다.
아래 신호가 함께 나타나면 **은닉 실행 패턴**으로 보고 G1 제외한다.

- 특정 OS에서만 동작하도록 분기(`process.platform !== 'win32'` 등)
- `-ExecutionPolicy Bypass`, base64 `-EncodedCommand`
- 창 숨김(`windowsHide`, `WindowStyle Hidden`, `WScript.Shell.Run(..., 0, $false)`)
- 분리 실행(`detached`) + 출력 무시(`stdio: 'ignore'`)
- 오류를 조용히 삼킴(`catch {}`, 빈 error 핸들러)
- 실패 시 대체 실행 경로 준비

**판별 단위는 블롭 하나가 아니라 함께 실려온 파일 한 벌이다 (2026-08-03 3차 교정)**

07-31 이후 이 게이트는 **"동일 블롭 SHA가 이름을 바꿔 반복 등장하는가"** 를 지표로 썼다. 08-03 실측에서 그 지표가 **바꾸기 쉬운 쪽이 아니라 바꿀 이유가 없는 쪽**을 지목하고 있었음이 드러났다.

같은 캠페인 계정의 저장소 8개를 파일 단위로 펼치자 전부 **세 파일 한 벌**이었다.

| 파일 | 8개 저장소에서 | 의미 |
|---|---|---|
| `*.exe` (저장소마다 이름만 다름) | **바이트 동일**(`79d051352df6`, 804,688B) | 갈아끼울 이유가 없는 쪽 |
| `libcurl.dll` | **8개 전부 다른 SHA·크기**(797,696 ~ 816,640B) | 저장소마다 갈아끼우는 쪽 |
| `gup.xml` | 내용 동일 — **WinGup(Notepad++ 업데이터) 설정 원문** | 정체를 드러내는 쪽 |

즉 해시 차단 목록에 exe만 올렸다면 **DLL 8종은 전부 통과**한다. 판별 요령을 아래로 넓힌다.

- **동반 파일을 먼저 본다.** 저장소 목적과 무관한 **설정 파일·런타임 DLL**은 실행 파일 자체보다 강한 신호다. 마크다운 스킬 저장소에 제3자 업데이터 설정(`gup.xml`)이 있을 이유는 없다.
- **불변인 것과 가변인 것을 갈라 본다.** 여러 저장소에 걸쳐 **무엇이 고정이고 무엇이 바뀌는지**를 대조한다. 고정된 쪽이 운반 수단, 바뀌는 쪽이 내용물일 수 있다.
- **저장소의 자기 주장과 파일 목록을 대조한다.** `package.json`이 *"No network, no dependencies"* 라고 적어 두고 **네트워크 라이브러리를 동봉**하면 그 불일치 자체가 근거다.
- **은닉이 노골적이지 않아도 판정한다.** 08-02의 `ratchet`은 정책 우회·base64·창 숨김을 썼지만, 08-03의 `humanizer-cli`는 **결백해 보이게 쓰인 런처**로 같은 곳에 도착했다 — 화면에 보이는 패널을 Node가 직접 그려서 **바이너리가 무엇을 하든 출력이 같도록** 설계했고, 주석이 *"이 폴더에 다른 exe를 떨어뜨려도 그대로 동작한다"* 고 스스로 밝힌다. **표시되는 출력과 실행되는 것이 설계상 분리돼 있으면** 은닉 신호 목록에 걸리지 않아도 G1 제외한다.

**끼워 넣은 것이 아니라 가져온 것을 보라 (2026-08-04 4차 교정)**

07-31 이후 이 게이트는 판별 축을 **exe 해시 → 파일 한 벌** 로 넓혀 왔다. 08-04 실측에서 **그 축 전부가 공격자가 자유롭게 갈아치울 수 있는 쪽**이었음이 드러났다.

같은 캠페인 계정의 저장소 2곳을 상류와 블롭 단위로 대조한 결과다.

| 캠페인 저장소 | 상류 원본 | 대조 결과 |
|---|---|---|
| `andrej-karpathy-skills` (549★) | `multica-ai/andrej-karpathy-skills` **199,077★** | `skills/karpathy-guidelines/SKILL.md` = `6a62d0441753`, `README.zh.md` = `1228d8ccbe8f` — **바이트 동일** |
| `ponytail-improved` (584★) | `DietrichGebert/ponytail` **94,717★ · MIT** | 스킬 파일 **6종 전부 바이트 동일**(`02c0712c8627` 등). 설명 문구도 글자까지 동일 |

**둘 다 GitHub fork가 아니다**(`fork:false`, `parent:none`) — "forked from" 배너가 뜨지 않는다. 바뀐 것은 README와 삼종세트(exe·DLL·`gup.xml`) 주입뿐이다.

즉 **저장소의 매력(내용물)은 진짜이고, 그것이 자기 것이 아니다.** 별이 빠르게 붙는 것도 설명된다 — 19.9만★ 원본의 파일을 담았으니 자연스러운 반응이며, **평가 대상과 별이 가리키는 대상이 어긋나 있다**. 새 후보를 받을 때 아래를 돌린다.

```bash
# ① 후보의 SKILL.md 블롭 SHA
gh api "repos/{owner}/{repo}/git/trees/HEAD?recursive=1" \
  --jq '.tree[]|select(.path|endswith("SKILL.md"))|"\(.sha[0:12]) \(.size) \(.path)"'

# ② 설명 문구를 그대로 검색해 상위 저장소를 찾는다 (가장 싼 1단계 —
#    08-04의 두 건 모두 description이 원본과 글자까지 같았다)
gh search repos "{description 원문}" --sort stars --limit 5 \
  --json fullName,stargazersCount,createdAt,description

# ③ 상류에서 같은 값을 뽑아 대조하고, fork 여부를 확인한다
gh api "repos/{owner}/{repo}" --jq '"fork:\(.fork) parent:\(.parent.full_name // "none")"'
```

**판정 규칙**: 후보의 마크다운 블롭이 상위 저장소와 **동일한데 fork 관계가 없으면**, 실행 파일이 하나도 없어도 **계보 은폐로 보고 G1 검토 대상**에 올린다. 라이선스가 MIT라 복제 자체는 위반이 아닐 수 있다 — 문제는 복제가 아니라 **계보를 지운 것**이다.

**상류가 대형 저장소라는 보장은 없고, 별 수는 방향까지 뒤집힌다 (2026-08-05 실측으로 보강)**

08-04에는 표본 2건(19.9만★·9.5만★)만 있어 "초대형 저장소를 노린다"는 인상이 생겼다. 상류 대조를 4건으로 넓히자 둘 다 틀렸다.

| 캠페인 저장소 | 상류 | 대조 |
|---|---|---|
| `yoinks` **53★** | `pablostanley/yoinks` **1,240★** MIT | `src/` 12개 + `package.json`·`package-lock.json` 전부 동일. 다른 건 README뿐 |
| `agents-council` **295★** | `team-attention/agent-council` **139★** MIT (2025-12-14 생성, 7개월 앞섬) | 마크다운·설정·라이선스 10개 전부 동일. **교체된 건 `bin/install.js`(8,777B → 10,324B)** 하나, 추가된 건 exe·DLL·`gup.xml` |

두 가지가 따라온다.

- **상류는 139★일 수도 있다.** 규모로 후보를 좁히지 말 것.
- **복제본이 원본보다 별이 많을 수 있다**(295★ vs 139★). 검색 결과에서 **별이 많은 쪽을 원본으로 짐작하는 습관이 정확히 반대로 작동한다.** 판별은 별이 아니라 **생성일 + 블롭 SHA + fork 플래그**로 한다.

**교체된 파일 하나를 보라.** `agents-council`은 내용물을 손대지 않고 **로더만 갈아 끼웠다**. "무엇이 추가됐나"뿐 아니라 **"상류와 무엇이 달라졌나"** 를 함께 본다.

### 스냅샷 스캔은 어제를 증명하지 못한다 (2026-08-05 신설)

2026-08-04, 이 원장이 지목한 `ratchet`이 **강제 푸시로 exe·DLL·`gup.xml`을 삭제하고 커밋 이력을 1개로 축소**했다(다른 7곳은 3~9개). 오늘 바이너리 스캔을 돌리면 **8곳 중 유일하게 CLEAN**이다. 삭제된 블롭은 API로 404다.

그러나 남은 것이 더 중요했다.

- `bin/install.js`에 **08-02가 정리한 은닉 실행 신호 6종이 전부 잔존**하고, 135행이 여전히 `'hooks/ratchetui.exe is missing'`을 출력한다 — **로더는 지워진 exe를 계속 기다린다.**
- `hooks/lib/beacon.js`가 설치·CLI 명령 시점에 **`os.hostname()`과 상위 40단계까지 거슬러 찾은 저장소 폴더명**을 계정 소유 Cloudflare Worker로 POST한다. **README에 고지가 없다**(`beacon`·`telemetry`·`privacy`·`opt-out` 전부 무매치). 오류는 조용히 삼킨다.

따라서 세 가지를 규칙에 넣는다.

1. **판정에 커밋 SHA와 조회 시각을 함께 기록한다.** 트리 스냅샷만으로는 사후에 재확인이 불가능하다.
2. **커밋 수가 비정상적으로 적으면 이력 재작성을 의심한다.** 파일 수·생성일에 비해 커밋이 1~2개면 신호다.
3. **한 번 지목한 대상은 현재 상태로 사면하지 않는다.** "지금 스캔이 깨끗하다"는 "과거에 깨끗했다"가 아니다. 그리고 **정리된 뒤에 남은 파일을 전수로 다시 읽는다** — 크고 눈에 띄는 것이 치워지면 시선이 따라 움직인다.

**뒤집어 말하면, 상류 계보를 스스로 밝힌 산출물은 신뢰도 가점 대상이다** (2026-08-04 #5 `grill-for-unknowns`: `NOTICE.md` + `references/upstream-lineage.md`로 상류 3종의 URL과 차용 범위를 항목별로 공개). 계보를 안 밝힌 산출물은 나쁘다기보다 **중복·표절 판정을 불가능하게 만든다**. 따라서 **중복 판정의 기준은 "주제가 같은가"가 아니라 "상류가 같은가"** 다.

**08-03 관측 정정 (2026-08-04)**: 08-03은 `gup.xml`이 *"8개 전부 내용이 같다"* 고 적었다. **2변종이다.**

| 블롭 | 크기 | 저장소 |
|---|---|---|
| `bd59041f343b` | 4,608B | `agents-council`(2경로) · `andrej-karpathy-skills` · `yoinks` · `FlashKDA` |
| `c61c3c179a8d` | 4,521B | `openclaude-improved` · `ponytail-improved` · `ratchet` · `humanizer-cli` |

생성일 순서와도 일치하지 않는다(`FlashKDA` 07-29가 구변종, `openclaude-improved` 07-26이 신변종). **"고정된 것 vs 바뀌는 것" 이분법 자체가 너무 거칠었다.** 판정(전건 G1 제외)은 바뀌지 않는다.

**바이너리는 여전히 받지도 돌리지도 않는다.** 위 판정은 전부 트리·블롭 메타데이터와 텍스트 파일 원문만으로 가능하며, 악성 여부는 단정하지 않는다.

**양방향 실측 사례(2026-08-02)**

- **제외** — `0xwilliamortiz/ratchet`: `hooks/ratchetui.exe`(불투명)를 `bin/install.js`가 위 신호 여섯 개를 모두 갖춘 방식으로 실행. 정상 UI 실행기에는 필요 없는 조합이다.
- **오탐** — `microsoft/skill-recorder`: `install.ps1`이 걸렸으나 **읽어 보니** 미리 빌드된 앱을 받지 않고 공식 포터블 런타임 + 지정 소스 커밋 + lockfile 고정 의존성 + 라이선스 검증 후 **로컬 빌드**. 제외 사유 아님(설치 부담은 G4로 별도 판정).

**계정 스캔 결과를 읽는 법 — 2단계 판정 (2026-08-01 오탐 교정)**

계정 스캔에 뭔가 걸렸다고 바로 제외하지 않는다. 이 게이트가 잡으려는 것은
"계정에 실행 파일이 있다"가 아니라 **"동일한 블롭 하나가 여러 저장소에 이름을 바꿔 반복 등장한다"** 이다.

| 관측 | 판정 |
|---|---|
| **후보 저장소 본체**에 서명 없는 실행 파일 | **G1 제외** |
| 여러 저장소에 **동일 블롭 SHA가 다른 이름으로** 반복 | **G1 제외(캠페인)** |
| 무관한 fork·오래된 저장소의 **서로 다른 제3자 바이너리** | **오탐 — 제외 사유 아님** |

확인 순서: ① 후보 저장소 본체를 다시 스캔한다 ② 걸린 저장소가 **fork인지, 생성일이 후보와 무관하게 오래됐는지** 본다
③ **블롭 SHA를 비교**해 재사용 패턴인지 확인한다. 셋 다 아니면 오탐으로 판정하고 후보를 살린다.

실제 오탐 사례(2026-08-01): `AmazingAng/old-coder` 심사 중 같은 계정의 `BNT`에서 `.exe`·`.dll` 16개가 걸렸으나,
**2019-12-30 생성 fork**에 담긴 표준 xpdf 유틸리티와 계측 장비 DLL이었고 **SHA가 전부 상이**했으며
후보 저장소 본체는 CLEAN이었다. → 오탐, `old-coder` 채택.

### G3 통과 목록 — 산출물 유형별 (2026-07-31 개정)

이 리포트가 추천하는 것의 상당수는 프로그램이 아니라 **문서**(마크다운 스킬·프롬프트·규칙 파일)다.
문서 저자는 코드용 라이선스 대신 크리에이티브 커먼즈를 쓰는 경우가 많아, 코드용 목록만 적용하면
**조건상 아무 문제 없는 문서형 산출물이 목록에 없다는 이유로 탈락**한다(2026-07-31 `harness-engineering` 2,408★ 사례).
따라서 통과 목록을 유형별로 나눈다.

| 산출물 유형 | 통과 | 제외 |
|---|---|---|
| **코드** (실행되는 것) | MIT · Apache-2.0 · BSD · ISC | GPL/AGPL/상용/독점/불명, **CC 계열 전부**(코드용으로 설계되지 않음) |
| **문서** (마크다운·프롬프트·규칙) | MIT · Apache-2.0 · BSD · ISC<br>**+ CC0-1.0 · CC-BY-4.0** | GPL/AGPL/상용/독점/불명<br>**+ CC-BY-NC · CC-BY-ND · CC-BY-SA** |

CC 계열을 통째로 열지 않는 이유는 **종류마다 조건이 다르기 때문**이다.

| 종류 | 조건 | 판정 |
|---|---|---|
| **CC0-1.0** | 저작권 포기. 출처 표기 의무도 없음 | ✅ 통과 |
| **CC-BY-4.0** | 출처만 밝히면 사용·수정·상업적 이용 자유 | ✅ 통과 |
| CC-BY-**NC** | **비영리 목적만** 허용 | ❌ 회사 업무 사용은 조건 위반 |
| CC-BY-**ND** | **개작 금지** | ❌ 스킬은 수정해서 쓰는 것이므로 실질적으로 무용 |
| CC-BY-**SA** | 수정본 배포 시 **동일 조건 공개 의무** | ❌ 사내 문서에 섞으면 공개 의무가 전염될 수 있음 |

### 문서형 통과분의 부수 의무

1. **출처 표기(CC-BY 채택분에 한함).** 가져온 파일 상단에 출처 한 줄을 남긴다.
   `<!-- 출처: {저작자}, {저장소 URL}, CC BY 4.0 -->` 정도면 충분하다.
   CC0은 표기 의무가 없으나 추적을 위해 같은 형식을 권장한다.
2. **개작 사실 명시(CC-BY).** 원문을 고쳐 쓰면 "수정함"을 표시해야 한다.
3. **혼합 저장소는 부분별로 판정한다.** 문서와 실행 코드가 함께 있으면
   **문서 부분만 CC 통과로 인정**하고, 코드 부분은 코드용 라이선스를 별도 확인한다.
   확인되지 않으면 **그 코드는 가져오지 않는다**(저장소 전체를 탈락시키는 것이 아니라, 취할 범위를 줄인다).
4. **저장소 루트 LICENSE가 하위 경로를 덮지 않는다.** `sources/`·`vendor/`처럼
   제3자 원문을 벤더링한 경로는 자체 라이선스를 따르므로 그 경로의 라이선스 파일을 확인한다.

### `NOASSERTION`은 판정이 아니라 확인 요청이다 (2026-07-31 신설)

GitHub API의 라이선스 필드가 `NOASSERTION`이면 **"불명"으로 자동 탈락시키지 말고 LICENSE 파일 원문을 읽는다.**
2026-07-31 실측에서 양방향으로 확인됐다.

- `finding-unknowns-skills`·`anthropics/code-migration-kit` — API는 `NOASSERTION`이었으나 파일은 **표준 MIT·표준 Apache-2.0**이었다. 필드만 보고 걸렀다면 벤더 공식 킷을 "라이선스 불명"으로 버렸을 것이다.
- `openclaude-improved` — 파일이 **"Claude Code CLI 파생, 원본은 독점 소프트웨어"라는 NOTICE**였다. 제외는 옳았고, 사유는 "불명"이 아니라 **"독점 파생물"이라는 더 강한 근거**였다.

즉 `NOASSERTION`은 통과도 탈락도 아니고 **원문을 읽으라는 신호**다.

## 채택 등급

통과한 항목만 아래로 등급을 매기고, **등급 우선 → 주목도순**으로 정렬한다.

| 등급 | 정의 | 예 |
|---|---|---|
| **S** | **이식형 스킬·설정.** `SKILL.md`·프롬프트·마크다운·설정 파일만으로 기존 하네스에 얹힘. 설치 0 | 스킬팩, 슬래시 커맨드, 훅 설정, 규칙 문서 |
| **A** | **무설치 활용.** 이미 있는 런타임으로 동작(예: stdlib만 쓰는 단일 파일 스크립트). 새 프로그램 없음 | 의존성 0 파이썬 스크립트, 순수 설정 |
| **B** | 설치 필요 | **기본 제외** |

**S가 최우선이다.** 같은 주목도면 S를 A보다, A를 나머지보다 위에 둔다.

## B등급 예외 (엄격)

설치가 필요한 항목은 원칙적으로 싣지 않는다. 다음을 **전부** 만족할 때만 예외로 싣고,
반드시 `[설치 필요]` 표기와 함께 **왜 예외인지**를 적는다.

1. 대체 가능한 S/A등급 수단이 없고,
2. 개인 스택 설계 자체에 영향을 주는 사안이며(예: 쓰던 프레임워크의 EOL),
3. G1~G3은 통과했을 것.

즉 "좋은 도구인데 설치가 필요하다"는 예외 사유가 되지 않는다.
"내가 쓰던 게 죽었으니 갈아타야 한다" 같은 **의사결정 정보**만 예외다.

## 로컬 모델의 취급 — "출시 소식"과 "선택 근거"를 가른다 (2026-08-03 사용자 판단으로 신설)

범위표는 **"모델 출시 소식 자체"를 제외**하면서 동시에 **"로컬/셀프호스팅 스택"은 포함**한다.
이 둘이 충돌하는 지점이 실제로 있었다 — *"이 장비에서 무엇을 돌릴 수 있는가"* 는 출시 뉴스가 아니라 **스택 결정**인데,
"모델 관련"이라는 이유로 통째로 걸러지고 있었다(2026-08-03 사용자 지적으로 확인).

| 유형 | 예 | 판정 |
|---|---|---|
| **모델 출시 소식 자체** | 새 모델 공개, 파라미터 수, 벤치 점수 자랑, 라이선스 변경 | **계속 제외** |
| **선택 근거** | 주어진 VRAM·RAM에서 **실제로 도는가**, 양자화별 품질·속도 실측, 메모리 천장, 서빙 경로의 함정, 로컬↔원격 라우팅 판단 | **채택 대상**(분류: 로컬 스택) |

가르는 기준은 **"이 항목이 내 장비 구성을 바꾸는가"** 이다. 바꾸면 싣고, 소식이면 뺀다.

### 로컬 항목에 대한 G4 재해석

가중치 다운로드를 "설치"로 세면 로컬 스택 항목은 전부 B등급이 되어 규칙이 무의미해진다.
따라서 로컬 항목에 한해 G4를 **"이미 깔린 런타임에서 도는가"** 로 판정한다.

| 관측 | 등급 |
|---|---|
| 이미 있는 런타임(llama.cpp·Ollama 등)에서 **설정·가중치만으로** 동작 | **A**(가중치 다운로드는 설치로 세지 않는다) |
| 마크다운·스킬·체크리스트 형태의 **선택 근거 문서** | **S** |
| **새 엔진·바인딩을 빌드하거나 링크**해야 함(C/C++/Rust 소스, pip 의존성 추가) | **B** — 원칙 제외, 아래 예외만 |

**B등급 예외가 로컬 항목에서 실제로 발동하는 경우**: *"내 메모리에서 무엇이 가능한가"* 의 **천장을 바꾸는 실측**은
문서로 대체할 수 없고 장비 구성 자체를 바꾸므로, 기존 예외 3조건(대체 S/A 없음 · 스택 설계에 영향 · G1~G3 통과)을 만족한다.
이때도 **`[설치 필요]` 표기와 왜 예외인지**를 반드시 적는다.

## 뉴스·방법론 항목의 취급

사건·논쟁·방법론은 설치 대상이 아니므로 G4·등급을 적용하지 않는다.
다만 **그 항목이 특정 도구 도입을 권하는 형태라면**, 그 도구에 G1~G4를 적용한다.

보안 사건 자체는 계속 다룬다 — 스택을 **어떻게 설정할지**를 바꾸는 정보이기 때문이다.
G1이 막는 것은 "보안 이슈가 있는 도구를 추천하는 것"이지 "보안 주제를 다루는 것"이 아니다.

## 취재 종결 규칙 (2026-08-05 사용자 지시로 신설)

**한 대상은 결정이 나오면 종결한다.** 이 리포트의 산출물은 *"오늘 내 스택에 무엇을 붙이고 무엇을 막을까"* 이지
특정 대상의 경과 보고가 아니다. 판정이 내려진 뒤의 후속 관측은, **그 판정을 뒤집거나 사용자의 행동을 바꿀 때만** 싣는다.

### 재등장 심사 — 아래를 통과해야 다시 싣는다

| 질문 | 통과 | 불통과 |
|---|---|---|
| 이 관측으로 **사용자가 다르게 행동하는가?** | 채택/제외가 뒤집힘, 새 차단 대상, 설정 변경 | "상태가 이렇게 바뀌었다" |
| 이 관측이 **판별 방법을 바꾸는가?** | 게이트·규칙 개정으로 이어짐 | 기존 규칙의 재확인 |
| 같은 대상을 **연속 몇 판째 싣는가?** | 2판까지 | **3판 이상이면 원칙 제외** |

셋 중 앞의 둘이 모두 "불통과"면 싣지 않는다. 세 번째는 앞의 둘이 통과해도 **연속 3판째부터는 제외를 기본값**으로 두고,
그럼에도 실어야 한다면 **왜 예외인지를 항목 안에 적는다.**

### 종결된 것은 원장에만 남긴다

판정이 끝난 대상의 후속 변화는 `RE-ADJUDICATION.md`에 한 줄로 기록하고 리포트 본문에는 올리지 않는다.
사용자가 물으면 원장을 근거로 답하면 된다. **기록과 게재는 다른 일이다.**

### 왜 이 규칙이 필요했나 (2026-08-05 실측)

`0xwilliamortiz` 계정이 **6판 연속 1위**였다(07-31·08-02·08-03·08-04·08-05, 07-30에는 5위).
그중 스택 결정을 바꾼 것은 **07-31 한 건**이다 — "이 계정 저장소를 설치하지 마라, 패턴은 이렇게 잡는다".
그 뒤 나흘은 같은 결론에 붙은 세부(파일 구성 정정, 상류 대조, 증거 삭제 관측)였고,
**사용자가 할 일은 07-31 이후 한 번도 바뀌지 않았다.**

원인은 08-02에 넣은 *"지목 계정은 재스윕 목록에 고정한다"* 였다. **매일 돌리는 조사 절차를 만들면 매일 실을 것이 생긴다.**
조사 절차와 게재 기준을 분리하지 않은 것이 실수였다. 스캔은 계속 돌리되(배제 목록 유지), **결과는 판정이 바뀔 때만 싣는다.**

## 개수 규칙 변경

**고정 TOP 10을 폐기한다.** 게이트를 통과한 것만 싣고, 최대 10건으로 제한한다.
통과 항목이 3건이면 3건짜리 리포트를 낸다.

이유: 엄격한 필터에 고정 개수를 얹으면 **채우기 위한 끼워넣기**가 발생한다.
이 저장소가 검증 규약으로 막으려는 실패와 같은 종류다.
통과 항목이 없는 날은 "오늘은 기준을 통과한 항목이 없음"이 정직한 결과다.

## 각 항목에 표기할 것

```
| 순위 | 트렌드 | 등급 | 라이선스 | 분류 | 관측 근거·시점 | 신뢰도 |
```

`등급`과 `라이선스`를 요약 순위표의 1급 컬럼으로 둔다.
라이선스를 확인하지 못했으면 그 항목은 애초에 실리지 않는다(G3).
